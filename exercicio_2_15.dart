/*
Verifique se as declarações de variáveis a seguir são válidas:
	a) j# === nao_valida
	b) abc === valida
	c) int === nao_valida
	d) m_2 === valida
	e) endereco === valida
	f) $mon === nao_valida
	g) J2P === valida
	h) 3var === nao_valida
	i) val*or === nao_valida
	j) a x === nao_valida
	k) S P === nao_valida
	l) S2P === valida
*/